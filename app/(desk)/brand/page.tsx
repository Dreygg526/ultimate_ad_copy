// Prep: research docs → bucket → text extracted → injected into every rebuild.
export default function BrandPage() {
  return (
    <main style={{ padding: 24 }}>
      <p className="eyebrow">Brand</p>
      <p style={{ color: 'var(--ink-soft)', maxWidth: '52ch' }}>
        Brand, audience, and mechanism research grounds every rebuild. Upload lands
        here once storage is provisioned.
      </p>
    </main>
  );
}
